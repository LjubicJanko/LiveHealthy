template header

myRule
eventType

package rules.dynamic;


global live.healthy.facts.model.dynamic.AlertDecision alertDecision;

dialect "java"
template "alert"

rule "naziv pravila"

when
    @{eventType}(@{myRule})
then
    alertDecision.setDoAlert(Boolean.TRUE);
    System.out.println("Print unutar pravila");
end

end template

