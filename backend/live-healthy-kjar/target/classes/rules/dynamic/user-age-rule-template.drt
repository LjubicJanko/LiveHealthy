template header
minAge
maxAge

package rules.dynamic;

import live.healthy.facts.model.user.User;

template "user-rules"

rule "Age range_@{row.rowNumber}"
no-loop true
when
    $u: User(age >= @{minAge}, age <= @{maxAge})
then
    modify ($u){ setAge(77) };
end

end template